export interface Product {
  id: number;
  title: string;
  slug: string;
  isActive: boolean;
  lpDescription: string;
  lpContent: string;
  lpStyles: string;
  lpScripts: string;
  createdAt: Date;
}
